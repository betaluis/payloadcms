import breakpoints from './breakpoints';

export const xs = `@media(max-width: ${breakpoints.xs}px)`;

export const s = `@media(max-width: ${breakpoints.xs}px)`;

export const md = `@media(max-width: ${breakpoints.xs}px)`;

export const l = `@media(max-width: ${breakpoints.xs}px)`;

export const xl = `@media(max-width: ${breakpoints.xs}px)`;

export const xxl = `@media(max-width: ${breakpoints.xs}px)`;

export default {
  xs,
  s,
  md,
  l,
  xl,
  xxl,
};
